# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
dist/
build/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
# Vite build output (run: cd frontend && npm run build)
hosaka/web/ui/

# Electron kiosk host — node_modules is platform-specific (darwin vs linux-arm64)
# so we NEVER commit it. `npm install` re-creates it on the target, with the
# right native Electron binary pulled by the `electron` package's postinstall.
kiosk/node_modules/
kiosk/out/
kiosk/dist/

# env
.env*
!frontend/.env.appliance

# OS
.DS_Store

# IDE
.vscode/
.idea/

# keys
*.pem
.env
