{
  "name": "hosaka-install-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that powers install.hosaka.xyz.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
